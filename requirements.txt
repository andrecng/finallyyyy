# Core
fastapi==0.110.0
uvicorn[standard]==0.30.1
numpy==1.26.4
pydantic==2.8.2
requests==2.32.3
pytest==8.3.2
httpx==0.28.0

# Optionnel / Windows-only (broker bridge). Jamais installé en CI Linux.
MetaTrader5>=5.0.0; platform_system == "Windows" and python_version < "3.11"


